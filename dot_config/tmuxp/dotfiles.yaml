session_name: Dotfiles 
start_directory: ~/
windows:
  - window_name: config 
    options:
      main-pane-width: 80%
    layout: main-vertical
    start_directory: ~/.config
    panes:
      - shell_command:
          - chezmoi edit .
      - shell_command:
          - chezmoi cd
          - git fetch
