dot_pi/agent/auth.json
dot_pi/agent/sessions/
node_modules/

# Editor-only TS workspace artifacts (regenerated by `npm install`).
# package.json + tsconfig.json are tracked; lockfile is local-only churn.
dot_pi/agent/extensions/package-lock.json
