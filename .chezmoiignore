.DS_Store

# Pi runtime state — never deploy
.pi/agent/auth.json
.pi/agent/sessions
.pi/agent/extensions/**/node_modules

# Editor-only TS workspace files — used here in ~/code/dotfiles/dot_pi/...
# for vtsls type resolution; pi runtime in ~/.pi/ does not need them.
.pi/agent/extensions/package.json
.pi/agent/extensions/package-lock.json
.pi/agent/extensions/tsconfig.json

# Repo-meta files live in ~/code/dotfiles/, not ~/
README.md
DOTFILES_POLICY.md
docs
install.sh
packages.txt

# Agent rules + shared skills are read in place from ~/code/dotfiles/agent-coding/;
# not deployed to ~/agent-coding (all references point at the repo path directly).
agent-coding

{{ if ne .chezmoi.os "darwin" }}
.config/aerospace
.config/wezterm
.config/ghostty
{{ end }}
